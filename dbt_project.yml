name: hubspot_technical_assessment
version: '1.0'
config-version: 2

profile: hubspot_technical_assessment_profile

source-paths: ["models"]
macro-paths: ["macros"]
seed-paths: ["seeds"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets: ["target", "dbt_modules"]

models:
  hubspot_technical_assessment:
    +materialized: view
    staging:
      +schema: staging
    marts:
      +schema: marts
      +materialized: table
